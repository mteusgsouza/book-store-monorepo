model User {
  id        String   @id @default(uuid())
  kindeId   String   @unique
  firstName String?
  lastName  String?
  email     String?
  picture   String?
  createdAt DateTime @default(now())
  posts     Post[]
}
